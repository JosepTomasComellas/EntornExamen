# /etc/nginx/conf.d/00-maps.conf
# Necessari per al suport WebSocket (Blazor SignalR / qualsevol app amb WS).
# La directiva map ha d'estar al context http, per això va a conf.d/.

map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
